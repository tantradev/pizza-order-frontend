import { createMuiTheme } from '@material-ui/core/styles'
const theme = createMuiTheme({
    palette: {
        primary: { main: '#29CCA0' },
        blue: { main: '#467fcf' },
        black: { main: '#000' },
        white: { main: '#fff' },
        red: { main: '#ff0000' },
    },
})
export default theme